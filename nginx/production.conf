server {
    listen 7860;
    server_name localhost;

    location /static/ {
        alias /app/staticfiles/;
        autoindex off;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


# server {
#     listen 80;
#     server_name yourdomain.com www.yourdomain.com;  # Replace with your domain

#     # Redirect HTTP to HTTPS (optional but recommended)
#     return 301 https://$host$request_uri;
# }

# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com www.yourdomain.com;

#     # SSL configuration - replace with your cert paths
#     ssl_certificate /etc/ssl/certs/your_cert.pem;
#     ssl_certificate_key /etc/ssl/private/your_key.pem;

#     # Security headers
#     add_header X-Frame-Options SAMEORIGIN;
#     add_header X-Content-Type-Options nosniff;
#     add_header X-XSS-Protection "1; mode=block";
#     add_header Referrer-Policy no-referrer-when-downgrade;
#     add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline';";

#     # Enable HSTS (HTTP Strict Transport Security)
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

#     # Serve static files directly
#     location /static/ {
#         alias /home/ashutosh/Desktop/CreatorsOmniverse/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, must-revalidate, proxy-revalidate";
#     }

#     # Pass all other requests to Django via Gunicorn or your WSGI server
#     location / {
#         proxy_pass http://127.0.0.1:8000;  # Your Django Gunicorn or uwsgi server
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
